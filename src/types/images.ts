export enum DPMImages {
  NoTransaction,
  NoProfile,
  Success,
  Fail,
  NoConnection,
  ConnectChain,
  ConnectMnemonic,
  ConnectLedger,
  PasswordSuccess,
  Ledger,
}

export enum DPMAnimations {
  BroadcastTx,
  LookingForDevices,
  ConnectToLedger,
  Loading,
}
